# Variáveis de Ambiente para AllureModa

# --- Banco de Dados (PostgreSQL) ---
# Substitua com a sua string de conexão do PostgreSQL.
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:admin@localhost:5432/alluremoda"

# --- Autenticação (JWT) ---
# Chave secreta para assinar os tokens JWT.
# Use uma string longa, complexa e aleatória para segurança.
# Você pode gerar uma usando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="seu-segredo-super-secreto-aqui"

# --- Frontend ---
# URL base do seu aplicativo frontend.
# Essencial para a configuração do CORS, permitindo que o frontend
# se comunique com a API.
FRONTEND_URL="http://localhost:3000"

# --- Servidor ---
# Porta em que o servidor backend irá rodar. (Opcional, padrão é 3001)
# PORT=3001
